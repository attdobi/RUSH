/* Preserve the original About page verbatim. Append explicit methodological
 * amendments instead of replacing its architecture, equations or research agenda. */
(() => {
  const html=`<details id="researchAddendum"><summary>Research amendments — GenAI, MNIST, label repair, and the KDD study design</summary>
  <p>The original research document above is preserved. These amendments distinguish the existing implementation from the next experiments; they do not rewrite historical runs.</p>
  <h3>1. Two changing objects: policy and reference labels</h3>
  <p>Version the policy graph G and the golden-label snapshot L separately. When an expert corrects a label, recompute both the incumbent and candidate against the same L before comparing them. Otherwise a score can improve because the yardstick changed rather than because the policy improved. Keep the original label, adjudication context, reviewer visibility, and policy version. Confirmations obtained after showing the panel answer are not independent votes.</p>
  <h3>2. MNIST: test whether a rule transfers beyond its anchors</h3>
  <p>A 4/9 boundary edit should be evaluated on untouched 4s and 9s across handwriting styles, plus unaffected digits as regression controls. Separate source images before applying transformations so related images do not cross splits. The existing expression <code>p = c if correct, otherwise 1 − c</code> is not the probability of the true digit in a ten-class problem: when a wrong class receives probability c, 1 − c is the combined probability of all other classes. Without a full class distribution, treat this as a confidence/error ranking heuristic—not calibrated multinomial cross-entropy or a literal gradient of the prompt.</p>
  <h3>3. GenAI: define the label before optimizing the classifier</h3>
  <p>Define whether generative editing, inpainting, conventional CGI, and hybrids count as positive. Distinguish source-generation provenance from visual-artifact evidence. A genuine generated image may look photographic; a camera image may contain unusual anatomy, compression or stylized content. An honest unknown/abstain route is needed when the available evidence cannot resolve the operational definition. Evaluate leave-generator-family-out, source-family-out and temporal transfer—not just a random image split from the same sources.</p>
  <h3>4. Identify the effect of the graph, rather than merely drawing one</h3>
  <p>Compare a frozen policy, flat-text optimization, and localized graph-node optimization under matched labels, judge panel, anchors and budget. Rendering the same prompt bytes from a graph cannot by itself establish a graph-quality gain; that is a representation-fidelity control. The testable mechanisms are localized edit search, protected exceptions, targeted evidence retrieval, and later explicit predicate execution. Random misalignment selection remains the control that ranked anchors must beat.</p>
  <h3>5. Generalization, label noise and acceptance</h3>
  <p>The repeatedly consulted gate set is development validation. A benchmark used to select configurations also becomes development evidence. Reserve a final, version-pinned reference set and source/temporal transfer set. Blindly re-adjudicate a stratified sample including unanimous, apparently correct decisions, not only disagreements. Report paired risk changes, per-slice FPR/FNR, decision coverage, adjudication cost, and all tried seeds. Empirical seed dispersion is a stability diagnostic; embedding distance between final documents alone does not estimate a Lyapunov exponent.</p>
  <h3>6. Decision paths have three different meanings</h3>
  <p>Log separately which rules were served, which rules a model cited, and which typed predicates actually executed. Citations are not causal attribution. Deterministic traversal is reproducible conditional on its supplied facts; it does not make visual extraction correct. To isolate routing's effect, compare full-policy and routed-policy judges on identical observations, and separately evaluate observation extraction against an expert reference. The decision-path sandbox remains shadow-only.</p>
  <h3>Scoring and confidence amendments</h3>
  <p>For positive denominator, F1 = <code>2TP/(2TP + FP + FN)</code>, including zero when TP = 0. The earlier branch corrected a scorer that could omit zero-F1 classes. Stored pre-correction scores require re-scoring; historical gate decisions remain audit records. The human-confidence function <code>1 − 1/(m + 4)</code> is a workflow weighting heuristic unless calibrated against independently adjudicated outcomes; it is not an established probability that a label is correct.</p>
  <p>Relevant textual-optimization comparator: <a href="https://arxiv.org/abs/2507.19457" target="_blank" rel="noopener">GEPA</a>. A comparison should match the available evidence and evaluation budget, not transfer reported paper gains to RUSH. This addendum introduces no new optimizer, model-weight training, or claimed experimental result.</p></details>`;
  function append(){const host=document.getElementById('aboutContent');if(!host||!host.querySelector('.about-doc')||host.querySelector('#researchAddendum'))return;host.insertAdjacentHTML('beforeend',html);}
  function init(){const host=document.getElementById('aboutContent');if(!host)return;new MutationObserver(append).observe(host,{childList:true});append();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
